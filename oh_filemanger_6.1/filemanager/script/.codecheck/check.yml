---
version: 2.0
params:
  - name: checkRoot
    value: .

steps:
  pre_codecheck:
    - checkout:
        path: HmFileManager #定义代码的存放目录
tool_params:
  secsolar:
    source_dir: ./products/phone/src/main/ets  #扫描目录，默认是代码仓根目录